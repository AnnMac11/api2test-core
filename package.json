{
  "name": "api2test-core",
  "version": "0.1.0",
  "description": "Shared engine for API2Test: import API specs, build a data dictionary, manage data/method/class libraries, and generate request-body classes and integration tests. UI-agnostic; consumed by the VS Code extension, the web/enterprise app, and (later) the Jira app.",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "resources"
  ],
  "scripts": {
    "build": "tsc -p ./",
    "clean": "rimraf dist",
    "test": "node --import tsx --test test/*.test.ts",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "yaml": "^2.8.1"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "rimraf": "^6.1.3",
    "tsx": "^4.23.1",
    "typescript": "^7.0.2"
  },
  "keywords": [
    "api",
    "testing",
    "openapi",
    "postman",
    "code-generation"
  ]
}
