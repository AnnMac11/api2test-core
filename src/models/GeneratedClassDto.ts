export interface GeneratedClassDto {
    id: string;
    className: string;
    code: string;
    endpoint: string;
    method: string;
    application: string;
    createdDate: string;
    namespace?: string;
}